#pragma once

#include <GL\glew.h>
#include <string>

const std::string GAME_NAME = "Puzzle Platformer";

const GLint WINDOW_WIDTH = 800;
const GLint WINDOW_HEIGHT = 600;

const unsigned int FPS = 60;
const unsigned int FRAME_TARGET_TIME = 1000 / FPS;